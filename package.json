{
  "name": "build_graphql-yoga",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --no-notify index.ts",
    "build": "tsc --outDir lib -p .",
    "build:fast": "tsc --skipLibCheck --outDir lib -p ."
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "ts-node": "10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@graphql-yoga/node": "^2.13.7",
    "graphql": "^16.5.0"
  }
}